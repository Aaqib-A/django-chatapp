version: '3.8'

services:
  img_chatapp-redis:
    image: redis:latest
    container_name: chatapp-redis
    ports:
      - "6379:6379"
    networks:
      - chatapp-network

# volumes:
#   chatapp-redis:

networks:
  chatapp-network: